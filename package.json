{
	"name": "@js-util/config-object-merge",
	"version": "1.0.2",
	"description": "Given two config like object, merge them together - think if it as a recrusive merge that does *not* merge arrays",

	"keywords": [
		"config",
		"object",
		"merge"
	],

	"main": "configObjectMerge.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},

	"author": "picocreator",
	"license": "MIT",
	"private": false,

	"repository": {
		"type": "git",
		"url": "https://github.com/js-util/config-object-merge.git"
	},
	
	"engines": {
		"node": ">=8.0.0"
	}
}
